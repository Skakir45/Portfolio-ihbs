services:
  - type: web
    name: ihbs-website
    env: static
    buildCommand: cd app && npm install && npm run build
    staticPublishPath: ./app/dist
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
